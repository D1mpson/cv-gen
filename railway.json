{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "./mvnw clean package -DskipTests=true"
  },
  "deploy": {
    "startCommand": "find target -name '*.jar' -exec java -jar {} \\;",
    "healthcheckEnabled": false
  }
}